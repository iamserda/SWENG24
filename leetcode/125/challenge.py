def isPalindrome(s: str) -> bool:
    """Given a string 's', return true if it is a palindrom, otherwise false."""
    
assert isPalindrome("A man, a plan, a canal: Panama") == True
assert isPalindrome("race a car") == False
